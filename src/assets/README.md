Place the hero image file provided by the user into this folder.

Filename expected by `src/components/Hero.jsx`:

Messenger_creation_BA6511BD-EFFD-4E06-BEC0-7437E9CE823E.jpeg

You can add optimized variants (webp/avif) later and update the component to use a `<picture>` with `srcSet` for responsive images.
